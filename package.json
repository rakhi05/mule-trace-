{
    "name": "money-muling-forensics",
    "version": "1.0.0",
    "description": "Financial Forensics Engine for Money Muling Detection",
    "scripts": {
        "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run backend\" \"npm run frontend\"",
        "backend": "cd backend && python -m uvicorn main:app --reload --port 8000",
        "frontend": "cd frontend && npm run dev",
        "install-all": "npm install && cd frontend && npm install && cd ../backend && python -m pip install -r requirements.txt",
        "generate-data": "cd backend && python generate_data.py --transactions 10000"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}